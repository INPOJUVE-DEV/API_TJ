{
  "name": "tarjeta-joven-backend",
  "version": "0.1.0",
  "description": "Backend API para el programa Tarjeta Joven",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "seed": "node scripts/seed.js",
    "db:ensure": "node scripts/ensure-schema.js",
    "seed:if-needed": "node scripts/seed-if-needed.js",
    "start:render": "npm run db:ensure && npm run seed:if-needed && npm start"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.0.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.1",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.7.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
